// @flow

export type ApiState = {
  announcers: object,
  client: object,
  torrentFiles: object,
  settings: object,
  stomp: object
};

/*export type ApiState = {
  announcers: AnnouncerState,
  client: ClientState,
  torrentFiles: TorrentFilesState,
  settings: SettingsState,
  stomp: StompState
};*/
